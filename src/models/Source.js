export default class Source {

    constructor(url, name, providesImage, replace) {
        this.url = url;
        this.name = name;
        this.providesImage = providesImage;
        this.replace = replace;
    }

}
